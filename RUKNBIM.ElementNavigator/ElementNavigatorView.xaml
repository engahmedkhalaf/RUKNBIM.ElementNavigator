<UserControl x:Class="RUKNBIM.ElementNavigator.ElementNavigatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RUKNBIM.ElementNavigator"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300"
             Background="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Paste Revit Element IDs (newline or comma separated):" 
                   TextWrapping="Wrap" 
                   FontWeight="Bold" 
                   Margin="0,0,0,5"/>

        <TextBox Grid.Row="1" 
                 Text="{Binding ElementIdsInput, UpdateSourceTrigger=PropertyChanged}" 
                 AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto" 
                 Margin="0,0,0,10"/>

        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,0,0,10">
            <CheckBox Content="Isolate Selected Elements" 
                      IsChecked="{Binding IsolateSelected}" 
                      Margin="0,0,0,5"/>
            <CheckBox Content="Zoom to Selected Elements" 
                      IsChecked="{Binding ZoomToSelected}" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Vertical">
            <Button Content="Search &amp; Highlight" 
                    Command="{Binding SearchCommand}" 
                    Height="30" Margin="0,0,0,5" />

            <Button Content="Reset Full Model" 
                    Command="{Binding ResetModelCommand}" 
                    Height="30" Margin="0,0,0,5" />
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Import Excel" Command="{Binding ImportExcelCommand}" Height="30" />
                <Button Grid.Column="2" Content="Export Report" Command="{Binding ExportReportCommand}" Height="30" />
            </Grid>
        </StackPanel>

        <!-- Footer with Logo and Support Info -->
        <Border Grid.Row="4" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Margin="0,15,0,0" Padding="0,10,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Image Source="Images/logo.png" Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                    <TextBlock FontSize="11" Margin="0,0,0,2">
                        <Hyperlink NavigateUri="https://www.ruknbim.com/" RequestNavigate="Hyperlink_RequestNavigate">
                            <TextBlock Text="www.ruknbim.com" FontWeight="SemiBold"/>
                        </Hyperlink>
                    </TextBlock>
                    <TextBlock FontSize="10" Foreground="Gray">
                        <Run Text="Support: "/>
                        <Hyperlink NavigateUri="mailto:support@ruknbim.com" RequestNavigate="Hyperlink_RequestNavigate">
                            <TextBlock Text="support@ruknbim.com"/>
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
